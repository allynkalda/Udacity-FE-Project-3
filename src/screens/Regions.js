import { SafeAreaView, ScrollView } from 'react-native'
import View from '../features/Regions'

export default function Regions() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  )
}