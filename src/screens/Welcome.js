import { SafeAreaView, ScrollView } from 'react-native'
import View from '../features/Welcome'

export default function Welcome() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  )
}