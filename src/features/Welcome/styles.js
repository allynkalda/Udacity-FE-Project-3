import { StyleSheet } from 'react-native'

const stylesFn = () => StyleSheet.create({
  welcome: {
    alignItems: 'center',
    display: 'flex',
    flex: 1,
    padding: 20,
  }
});

export default stylesFn
