import { StyleSheet } from 'react-native'

const stylesFn = () => StyleSheet.create({
  create: {
    alignItems: 'center',
    display: 'flex',
    flex: 1,
    padding: 20,
  },
  createText: {
    fontSize: 16,
    padding: 12,
    // fontWeight: 500
  }
});

export default stylesFn
