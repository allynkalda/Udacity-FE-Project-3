import { StyleSheet } from 'react-native'

const stylesFn = () => StyleSheet.create({
  list: {
    alignItems: 'center',
    display: 'flex',
    flex: 1,
    padding: 20,
  },
  noCustomerText: {
    padding: 24,
    fontSize: 14
  }
});

export default stylesFn
